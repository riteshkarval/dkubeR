# Dkube api server
#
# No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)
#
# OpenAPI spec version: 2.2.1.11
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' InferenceJobModel Class
#'
#' @field model 
#' @field version 
#' @field owner 
#' @field device 
#' @field deploy 
#' @field mcname 
#' @field serving_image 
#' @field transformer 
#' @field transformer_image 
#' @field transformer_project 
#' @field transformer-commitID 
#' @field transformer_code 
#' @field minReplicas 
#' @field maxConcurrentRequests 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
InferenceJobModel <- R6::R6Class(
  'InferenceJobModel',
  public = list(
    `model` = NULL,
    `version` = NULL,
    `owner` = NULL,
    `device` = NULL,
    `deploy` = NULL,
    `mcname` = NULL,
    `serving_image` = NULL,
    `transformer` = NULL,
    `transformer_image` = NULL,
    `transformer_project` = NULL,
    `transformer-commitID` = NULL,
    `transformer_code` = NULL,
    `minReplicas` = NULL,
    `maxConcurrentRequests` = NULL,
    initialize = function(`model`, `version`, `owner`, `device`, `deploy`, `mcname`, `serving_image`, `transformer`, `transformer_image`, `transformer_project`, `transformer-commitID`, `transformer_code`, `minReplicas`, `maxConcurrentRequests`){
      if (!missing(`model`)) {
        stopifnot(is.character(`model`), length(`model`) == 1)
        self$`model` <- `model`
      }
      if (!missing(`version`)) {
        stopifnot(is.character(`version`), length(`version`) == 1)
        self$`version` <- `version`
      }
      if (!missing(`owner`)) {
        stopifnot(is.character(`owner`), length(`owner`) == 1)
        self$`owner` <- `owner`
      }
      if (!missing(`device`)) {
        stopifnot(is.character(`device`), length(`device`) == 1)
        self$`device` <- `device`
      }
      if (!missing(`deploy`)) {
        self$`deploy` <- `deploy`
      }
      if (!missing(`mcname`)) {
        stopifnot(is.character(`mcname`), length(`mcname`) == 1)
        self$`mcname` <- `mcname`
      }
      if (!missing(`serving_image`)) {
        stopifnot(R6::is.R6(`serving_image`))
        self$`serving_image` <- `serving_image`
      }
      if (!missing(`transformer`)) {
        self$`transformer` <- `transformer`
      }
      if (!missing(`transformer_image`)) {
        stopifnot(R6::is.R6(`transformer_image`))
        self$`transformer_image` <- `transformer_image`
      }
      if (!missing(`transformer_project`)) {
        stopifnot(is.character(`transformer_project`), length(`transformer_project`) == 1)
        self$`transformer_project` <- `transformer_project`
      }
      if (!missing(`transformer-commitID`)) {
        stopifnot(is.character(`transformer-commitID`), length(`transformer-commitID`) == 1)
        self$`transformer-commitID` <- `transformer-commitID`
      }
      if (!missing(`transformer_code`)) {
        stopifnot(is.character(`transformer_code`), length(`transformer_code`) == 1)
        self$`transformer_code` <- `transformer_code`
      }
      if (!missing(`minReplicas`)) {
        stopifnot(is.numeric(`minReplicas`), length(`minReplicas`) == 1)
        self$`minReplicas` <- `minReplicas`
      }
      if (!missing(`maxConcurrentRequests`)) {
        stopifnot(is.numeric(`maxConcurrentRequests`), length(`maxConcurrentRequests`) == 1)
        self$`maxConcurrentRequests` <- `maxConcurrentRequests`
      }
    },
    toJSON = function() {
      InferenceJobModelObject <- list()
      if (!is.null(self$`model`)) {
        InferenceJobModelObject[['model']] <- self$`model`
      }
      if (!is.null(self$`version`)) {
        InferenceJobModelObject[['version']] <- self$`version`
      }
      if (!is.null(self$`owner`)) {
        InferenceJobModelObject[['owner']] <- self$`owner`
      }
      if (!is.null(self$`device`)) {
        InferenceJobModelObject[['device']] <- self$`device`
      }
      if (!is.null(self$`deploy`)) {
        InferenceJobModelObject[['deploy']] <- self$`deploy`
      }
      if (!is.null(self$`mcname`)) {
        InferenceJobModelObject[['mcname']] <- self$`mcname`
      }
      if (!is.null(self$`serving_image`)) {
        InferenceJobModelObject[['serving_image']] <- self$`serving_image`$toJSON()
      }
      if (!is.null(self$`transformer`)) {
        InferenceJobModelObject[['transformer']] <- self$`transformer`
      }
      if (!is.null(self$`transformer_image`)) {
        InferenceJobModelObject[['transformer_image']] <- self$`transformer_image`$toJSON()
      }
      if (!is.null(self$`transformer_project`)) {
        InferenceJobModelObject[['transformer_project']] <- self$`transformer_project`
      }
      if (!is.null(self$`transformer-commitID`)) {
        InferenceJobModelObject[['transformer-commitID']] <- self$`transformer-commitID`
      }
      if (!is.null(self$`transformer_code`)) {
        InferenceJobModelObject[['transformer_code']] <- self$`transformer_code`
      }
      if (!is.null(self$`minReplicas`)) {
        InferenceJobModelObject[['minReplicas']] <- self$`minReplicas`
      }
      if (!is.null(self$`maxConcurrentRequests`)) {
        InferenceJobModelObject[['maxConcurrentRequests']] <- self$`maxConcurrentRequests`
      }

      InferenceJobModelObject
    },
    fromJSON = function(InferenceJobModelJson) {
      InferenceJobModelObject <- jsonlite::fromJSON(InferenceJobModelJson)
      if (!is.null(InferenceJobModelObject$`model`)) {
        self$`model` <- InferenceJobModelObject$`model`
      }
      if (!is.null(InferenceJobModelObject$`version`)) {
        self$`version` <- InferenceJobModelObject$`version`
      }
      if (!is.null(InferenceJobModelObject$`owner`)) {
        self$`owner` <- InferenceJobModelObject$`owner`
      }
      if (!is.null(InferenceJobModelObject$`device`)) {
        self$`device` <- InferenceJobModelObject$`device`
      }
      if (!is.null(InferenceJobModelObject$`deploy`)) {
        self$`deploy` <- InferenceJobModelObject$`deploy`
      }
      if (!is.null(InferenceJobModelObject$`mcname`)) {
        self$`mcname` <- InferenceJobModelObject$`mcname`
      }
      if (!is.null(InferenceJobModelObject$`serving_image`)) {
        serving_imageObject <- CustomContainerModel$new()
        serving_imageObject$fromJSON(jsonlite::toJSON(InferenceJobModelObject$serving_image, auto_unbox = TRUE))
        self$`serving_image` <- serving_imageObject
      }
      if (!is.null(InferenceJobModelObject$`transformer`)) {
        self$`transformer` <- InferenceJobModelObject$`transformer`
      }
      if (!is.null(InferenceJobModelObject$`transformer_image`)) {
        transformer_imageObject <- CustomContainerModel$new()
        transformer_imageObject$fromJSON(jsonlite::toJSON(InferenceJobModelObject$transformer_image, auto_unbox = TRUE))
        self$`transformer_image` <- transformer_imageObject
      }
      if (!is.null(InferenceJobModelObject$`transformer_project`)) {
        self$`transformer_project` <- InferenceJobModelObject$`transformer_project`
      }
      if (!is.null(InferenceJobModelObject$`transformer-commitID`)) {
        self$`transformer-commitID` <- InferenceJobModelObject$`transformer-commitID`
      }
      if (!is.null(InferenceJobModelObject$`transformer_code`)) {
        self$`transformer_code` <- InferenceJobModelObject$`transformer_code`
      }
      if (!is.null(InferenceJobModelObject$`minReplicas`)) {
        self$`minReplicas` <- InferenceJobModelObject$`minReplicas`
      }
      if (!is.null(InferenceJobModelObject$`maxConcurrentRequests`)) {
        self$`maxConcurrentRequests` <- InferenceJobModelObject$`maxConcurrentRequests`
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "model": %s,
           "version": %s,
           "owner": %s,
           "device": %s,
           "deploy": %s,
           "mcname": %s,
           "serving_image": %s,
           "transformer": %s,
           "transformer_image": %s,
           "transformer_project": %s,
           "transformer-commitID": %s,
           "transformer_code": %s,
           "minReplicas": %d,
           "maxConcurrentRequests": %d
        }',
        self$`model`,
        self$`version`,
        self$`owner`,
        self$`device`,
        self$`deploy`,
        self$`mcname`,
        self$`serving_image`$toJSON(),
        self$`transformer`,
        self$`transformer_image`$toJSON(),
        self$`transformer_project`,
        self$`transformer-commitID`,
        self$`transformer_code`,
        self$`minReplicas`,
        self$`maxConcurrentRequests`
      )
    },
    fromJSONString = function(InferenceJobModelJson) {
      InferenceJobModelObject <- jsonlite::fromJSON(InferenceJobModelJson)
      self$`model` <- InferenceJobModelObject$`model`
      self$`version` <- InferenceJobModelObject$`version`
      self$`owner` <- InferenceJobModelObject$`owner`
      self$`device` <- InferenceJobModelObject$`device`
      self$`deploy` <- InferenceJobModelObject$`deploy`
      self$`mcname` <- InferenceJobModelObject$`mcname`
      CustomContainerModelObject <- CustomContainerModel$new()
      self$`serving_image` <- CustomContainerModelObject$fromJSON(jsonlite::toJSON(InferenceJobModelObject$serving_image, auto_unbox = TRUE))
      self$`transformer` <- InferenceJobModelObject$`transformer`
      CustomContainerModelObject <- CustomContainerModel$new()
      self$`transformer_image` <- CustomContainerModelObject$fromJSON(jsonlite::toJSON(InferenceJobModelObject$transformer_image, auto_unbox = TRUE))
      self$`transformer_project` <- InferenceJobModelObject$`transformer_project`
      self$`transformer-commitID` <- InferenceJobModelObject$`transformer-commitID`
      self$`transformer_code` <- InferenceJobModelObject$`transformer_code`
      self$`minReplicas` <- InferenceJobModelObject$`minReplicas`
      self$`maxConcurrentRequests` <- InferenceJobModelObject$`maxConcurrentRequests`
    }
  )
)
